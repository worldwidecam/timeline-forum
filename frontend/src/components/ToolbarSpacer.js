import React from 'react';
import { Toolbar } from '@mui/material';

const ToolbarSpacer = () => {
  return <Toolbar />; // This empty toolbar provides the exact spacing needed
};

export default ToolbarSpacer;
